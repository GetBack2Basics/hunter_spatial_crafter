# Implementation Plan: Havasu / Iceberg / GeoParquet Architecture Section in HTML Report

Add a dedicated architectural section and tab in `national_suitability_report.html` (via `runner/build_suitability_report.py`) explaining the cloud-native spatial data stack (**GeoParquet**, **Apache Iceberg**, and **Wherobots Havasu**), with citations to foundational whitepapers and an explanation of how Wherobots achieves high-speed spatial processing.

## User Review Required

> [!NOTE]
> The new report section will feature a 3-layer architecture visual component, an explanation of the Havasu/Iceberg/GeoParquet table format stack, hyperlinked citations to whitepapers/specs, and a breakdown of Wherobots' spatial query performance engine.

## Proposed Changes

### Report Builder Script

#### [MODIFY] [build_suitability_report.py](file:///c:/Projects/hunter_spatial_crafter/runner/build_suitability_report.py)

- **New Architectural Card Section**: Add a styled container card in `HTML_TEMPLATE` titled **"Cloud-Native Spatial Lakehouse Architecture: Havasu, Apache Iceberg & GeoParquet"**.
  - **Layer Breakdown & Table Structure**:
    - **GeoParquet**: Vector columnar file format storing geometries with embedded row-group bounding boxes (`bbox`).
    - **Apache Iceberg**: Open cloud data lakehouse format providing ACID transactions, time travel, schema evolution, and hidden partitioning.
    - **Havasu (Spatial Iceberg)**: Wherobots' spatial extension adding Hilbert curve/R-tree spatial indexing, spatial envelope metadata, and metadata-level partition pruning.
  - **Performance Pillars (Why Spatial Processing is so fast)**:
    - Zero-Scan Metadata Envelope Pruning (95%+ file I/O reduction).
    - Hilbert Curve & R-Tree Spatial Co-location.
    - Vectorized Spatial Execution Engine (Apache Sedona).
    - Distributed Parallel Spatial Join Algorithms ($O(N \log M)$ execution).
  - **Whitepapers & Specifications Reference Links**:
    - [Wherobots Havasu Table Format Documentation & Whitepaper](https://docs.wherobots.com/latest/concepts/havasu/)
    - [Apache Iceberg Specification v2/v3](https://iceberg.apache.org/spec/)
    - [OGC GeoParquet Specification v1.1](https://geoparquet.org/)
    - [Apache Sedona (GeoSpark) Research Paper (SIGMOD)](https://sedona.apache.org/)
- **New Tab in Provenance Card**: Add a 5th tab `Spatial Lakehouse Stack (Havasu / Iceberg / GeoParquet)` to the *Benchmarking, Data Provenance & Open Evidence Trail* card.

---

### Output HTML File

#### [MODIFY] [national_suitability_report.html](file:///c:/Projects/hunter_spatial_crafter/runner/national_suitability_report.html)

- Automatically generated by executing `runner/build_suitability_report.py`.

---

### Documentation

#### [NEW] [docs/implementation_plan.md](file:///c:/Projects/hunter_spatial_crafter/docs/implementation_plan.md)

- Persist implementation details in the project `docs/` folder per project guidelines.

## Verification Plan

### Automated Verification
- Run `python runner/build_suitability_report.py` via `run_command` and confirm clean execution without errors.
- Verify file size and existence of `runner/national_suitability_report.html`.

### Manual Verification
- Check generated HTML file to ensure all links (GeoParquet, Iceberg, Havasu, Sedona), cards, tabs, and styling render properly.
